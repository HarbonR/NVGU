5 Ћабораторна€ работа. 13 задание.
13. Ќаписать функцию sogl, котора€ возвращает 1, если символ, полученный функцией в качестве аргумента, €вл€етс€ 
согласной буквой русского алфавита, и 0 Ч в противном случае.
//====================================================================================================
#include <stdio.h> /// —тандартна€ библиотека —и ввода-вывода
#include <windows.h> /// Ѕиблиотека дл€ работы с системными функци€ми

/// ‘ункци€ возвращает 1, если символ, полученный функцией в качестве аргумента, €вл€етс€ согласной буквой русского алфавита, и 0 Ч в противном случае
int sogl (char character)
{
    char consonants[] = "Ѕб¬в√гƒд∆ж«з…й кЋлћмЌнѕп–р—с“т‘ф’х÷ц„чЎшўщ"; /// —огласные буквы
    for (int i = 0; consonants[i] != '\0'; i++) /// ѕока не конец строки выполн€ть '\0'- “ернирующий ноль, означает конец строки
        if (character == consonants[i])
            return 1; /// ¬озвращаем 1
    return 0; /// »наче 0
}

int main()
{
    system("chcp 1251 > nul"); /// Ћокализуем русский €зык
    char character;
    printf ("¬ведите символ: ");
    character = getchar(); /// ‘ункци€ getchar возвращает символ введЄный с клавиатуры с отображением. ѕрисваиваем введЄный символ в переменную
    printf("%d", sogl(character));
    return 0;
}
//====================================================================================================